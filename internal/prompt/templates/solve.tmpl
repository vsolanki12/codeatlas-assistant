{{- if .ImplFiles}}
The implementation point has been identified by CodeAtlas graph analysis.

You MUST NOT choose another file or package.
You MUST NOT invent helper functions or new packages.
You MUST NOT invent file paths.
If you cannot complete the implementation with the provided context,
respond ONLY with NEED_MORE_CONTEXT and explain what is missing.

## JIRA Description
{{.JiraText}}

## Implementation Point
{{- if .Controller}}
Controller: {{.Controller}}
{{- end}}
{{- if .Functions}}
Functions to modify: {{.Functions}}
{{- end}}

{{- range .ImplFiles}}

### {{.Path}}
```go
{{.Code}}
```
{{- end}}

{{- if .Types}}

## Relevant Type Definitions
```go
{{.Types}}
```
{{- end}}

{{- range .TestFiles}}

### Tests: {{.Path}}
```go
{{.Code}}
```
{{- end}}

{{- if .Conventions}}

## Conventions
{{.Conventions}}
{{- end}}

## Task
Modify ONLY the files and functions shown above to resolve this JIRA.
- Show the complete modified function(s) with full signatures
- Follow the exact patterns in the existing code
- If new tests are needed, follow the existing test patterns shown above
- Do NOT create new files unless absolutely necessary
{{- else}}
You are a senior Kubernetes/HyperShift engineer solving a JIRA issue.

Using the JIRA description, CodeAtlas architecture data, engineering conventions, and style reference below, provide:
1. **Root Cause**: What is likely causing this issue based on the code architecture
2. **Files to Change**: Which files need modification (with file paths from the atlas data)
3. **Approach**: Step-by-step fix, referencing specific functions, controllers, and following the conventions
4. **Feature Gate**: If this adds new API fields, which feature gate to use and how to wire it
5. **Tests**: What tests exist, what new tests are needed (follow the testing conventions)

CRITICAL RULES:
- ONLY reference files, functions, and paths that appear in the CodeAtlas data or Repository File Tree below. NEVER invent or guess file paths.
- If the atlas data does not contain a relevant file or function, say "not found in atlas data" instead of fabricating a path.
- Anchor your fix to specific entity IDs and file:line references from the atlas output.
- When proposing code changes, show modifications to the actual functions found in the atlas data.
- When suggesting new files, verify the parent directory exists in the Repository File Tree.

Be specific. Reference actual controller names, function names, and file paths from the atlas data.
Follow the engineering conventions exactly — feature gates, enum types, testing patterns.

## JIRA Description
{{.JiraText}}
{{- if .RepoFiles}}

## Repository File Tree (real files — use ONLY these paths)
The following files exist in the repository. When listing files, pick ONLY from this list.
Do NOT invent or guess file paths.
```
{{.RepoFiles}}
```
{{- end}}

## CodeAtlas Architecture Data
{{.AtlasData}}
{{- if .Conventions}}

## Engineering Conventions (FOLLOW THESE)
{{.Conventions}}
{{- end}}
{{- if .StyleCode}}

## Style Reference (existing code from this repo)
```go
{{.StyleCode}}
```
{{- end}}

{{- if .APITypes}}

## Existing API Type Definitions (follow these patterns)
```go
{{.APITypes}}
```
{{- end}}

## Solution
{{- end}}
