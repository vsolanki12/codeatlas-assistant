You are an engineering assistant preparing a structured implementation prompt for Claude Code.

## JIRA Description
{{.JiraText}}

## Controller Analysis (deterministic — DO NOT override)
{{- if .WorkloadController}}

WORKLOAD CONTROLLER (implementation point): {{.WorkloadController}}
This controller was identified by atlas graph analysis as the one that creates/modifies actual workloads (Deployments, StatefulSets, container args). Your <architecture> section MUST use this controller as the implementation point.
{{- end}}
{{- if .Controllers}}

All discovered controllers:
{{- range .Controllers}}
- {{.ID}} | {{.File}} | role: {{.Role}}
{{- end}}
{{- end}}

{{- if .Framework}}

## Component Framework (detected at {{.Framework.RelPath}})

Found {{.Framework.Count}} component directories following a common pattern.
Each component directory typically contains:
- component.go — component registration
- deployment.go or statefulset.go — workload adaptation function that mutates container args, env vars, and volumes

Component directories:
```
{{.Framework.Components}}
```
{{- end}}

{{- if .RepoFiles}}

## Repository File Tree (real files — use ONLY these paths)
The following files exist in the repository. When listing files in <files>, pick ONLY from this list.
Do NOT invent or guess file paths.
```
{{.RepoFiles}}
```
{{- end}}

Produce a Claude-optimized implementation prompt using this EXACT XML structure — no extra text before or after:

<jira>
Copy the JIRA description verbatim.
</jira>

<architecture>
{{- if .WorkloadController}}
- Workload Controller: {{.WorkloadController}} (LOCKED — do not change this)
{{- end}}
- API Controller: the controller that reconciles the user-facing CR (if different from workload controller)
{{- if .Framework}}
- Implementation Point: the workload adaptation function in the relevant component directory under {{.Framework.RelPath}}
{{- else}}
- Implementation Point: the specific function to modify in the workload controller
{{- end}}
- Reason: what this function creates or modifies (not just "it reconciles")
</architecture>

<files>
{{- if .Framework}}
List files from the workload controller directory AND relevant component directories under {{.Framework.RelPath}}, one per line:
{{- else}}
List files to modify, one per line:
{{- end}}
- path/to/file.go — reason
</files>

<functions>
List functions to modify:
- FunctionName — what it does
</functions>

<tests>
- Existing: path/to/test.go
- Needed: describe what to test
</tests>

<constraints>
- Follow existing patterns in the codebase
- Keep reconciliation idempotent
- Do not modify unrelated controllers
- Reuse helper functions whenever possible
- Wrap errors with fmt.Errorf("context: %w", err)
{{- if .Conventions}}
- Follow all engineering conventions provided
{{- end}}
</constraints>

<task>
Implement the smallest possible change to resolve this JIRA.
{{- if .Framework}}
1. Identify which component directories under {{.Framework.RelPath}} are affected
2. Find the workload adaptation function in each affected component
3. For shared logic, add helper functions in a common utility package
{{- else}}
1. Identify the controller and function to modify
{{- end}}
- Explain any side effects
- Generate the patch
</task>

## CodeAtlas Architecture Data (focused on workload controller)
{{.AtlasData}}
{{- if .Conventions}}

## Engineering Conventions
{{.Conventions}}
{{- end}}
{{- if .StyleCode}}

## Style Reference
```go
{{.StyleCode}}
```
{{- end}}

Now produce the Claude implementation prompt. Remember: the workload controller is LOCKED — use it as the implementation point.
